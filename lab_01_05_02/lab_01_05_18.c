#include <stdio.h>
float Pow(float a,int n)
{
	float result=1;
	for (i;i++
}
int main()
{
	int n;
	float a;
	printf("Input a and n:\n ");
	scanf("%f%d",&a,&n);
	result = Pow(a,n);
	printf("Result: %f",result);
	return 0;
} 